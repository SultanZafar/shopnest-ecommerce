[project]
name = "shopnest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Django==6.1",
    "pillow==12.1.1",
    "psycopg2-binary",
]

[tool.vercel.scripts]
build = "python build.py"
